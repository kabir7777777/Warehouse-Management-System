# Warehouse-Management-System
A Python-based command-line shopping system that simulates a small retail workflow from user authentication to order fulfillment. The application supports secure user sign-up and login using hashed passwords (bcrypt), product lookup via CSV files, dynamic order pricing with discounts and tax, and persistent order history tracking.
